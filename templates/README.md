# Templates

This directory holds spreadsheet templates used for NCBI submissions.

## What to do

- Use these as inputs to `tools/generate_ncbi_submission.py`.
- If you update template versions, keep filenames consistent so the tooling can find them.
