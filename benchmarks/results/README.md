# Benchmark Results

Program outputs and scoring notes for benchmark runs.

## What to do

- Store one JSON object per line with `id`, `program`, `run_id`, and `response`.
- Optionally include `notes`, `calc`, `score`, and `latency_ms`.
