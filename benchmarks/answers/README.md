# Benchmark Answers

Gold/reference answers for benchmark queries.

## What to do

- Add one JSON object per line with matching `id` and `answer` payload.
- Keep `id` aligned with `benchmarks/queries/`.
