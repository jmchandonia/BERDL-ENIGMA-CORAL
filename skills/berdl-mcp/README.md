# Skill: berdl-mcp

Codex skill for interacting with the BERDL MCP API.

## What to do

- Read `SKILL.md` for the workflow and constraints.
- Use `references/` for schema and API details when present.
