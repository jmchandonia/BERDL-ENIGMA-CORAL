# Skill: enigma-berdl-query

Codex skill for querying ENIGMA CORAL data using the BERDL MCP API with strict schema constraints.

## What to do

- Read `SKILL.md` for the allowed tables/columns and query rules.
- Use `references/` for the canonical schema list.
