interface:
  display_name: "Sync CORAL to BERDL"
  short_description: "Run a verified, resumable CORAL-to-BERDL sync."
  default_prompt: "Use $sync-coral-to-berdl to run or resume a CORAL export sync, validate it, and publish the resulting schema references."
